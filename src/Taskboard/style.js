const styles = theme => ({
    taskboard: {

        alignItems: "center"
    },
    shape: {
        backgroundColor: theme.color.primary,
        color: "white",
        borderColor: "#cccccc",
        padding: 20,
        margin: 10,
        borderRadius: 4
    }
});

export default styles;
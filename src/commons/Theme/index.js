import { createMuiTheme } from '@material-ui/core/styles';
const theme = createMuiTheme({
    color: {
        primary: "#D32F2F",
        secondary: "#D32F2F",
        error: "#D32F2F"
    },
    typography: {
        fontFamily: "Roboto"
    },
    shape: {
        borderRadius: 4,
        background: "#D32F2F",
        textColor: "#ccc",
        border: "#D32F2F"
    }
});

export default theme;